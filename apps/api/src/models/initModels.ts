/**
 * Carregue e associe seus models aqui.
 * Quando você colocar seus models (Sequelize), chame .associate() onde existir.
 */
const initModels = async () => {
  // ex:
  // UserModel.associate?.();
};

export default initModels();
